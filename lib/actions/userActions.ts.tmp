"use server";

import { cookies } from "next/headers";
import { revalidatePath } from "next/cache";
import { prisma } from "@/lib/prisma";
import { Profile, USER_STATUS, UserStatus } from "@/lib/constants";
import { unlink } from "fs/promises";
import path from "path";

// ... rest of the file ...
// (I will use replace_file_content to fix the top part if I can, but write_to_file is safer if I have the whole content)
// Actually I don't have the whole content.
